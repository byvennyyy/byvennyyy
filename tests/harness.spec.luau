local T = require("./assert")
local shim = require("./_shim")
T.test("vector shim", function()
	local v = shim.Vector3.new(3, 4, 0)
	T.near(v.Magnitude, 5)
	T.near(v.Unit.X, 0.6)
end)
T.test("color shim", function()
	T.eq(shim.Color3.fromHex("#FF8000"):ToHex(), "FF8000")
end)
return T.finish()
